// A general grammar for representing a source file
// Every byte in source file should be in a 'Lin'

whitespace = { " " }

characters = {
  !(
    "\n" | "\r\n"
  )
  ~ any
}

newline = {
  "\n" | "\r\n"
}

line = { whitespace* ~ characters* ~ newline{1} }

file = { soi ~ line* ~ eoi }
